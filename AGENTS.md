# 한Q 작업 지침

- 작업 전 [개발 안내](docs/DEVELOPMENT.md)와 변경 대상의 [동작 명세](docs/SPEC.md)를 확인한다. 사용자 변경을 보존하고 요청 범위에서 작업한다.
- 사용자가 별도로 요청하지 않아도 [버전 관리 기준](docs/VERSIONING.md)에 따라 버전 갱신 필요성을 판단하고 `version.env`와 CHANGELOG를 함께 관리한다. 문서 수정·동일 입력의 재빌드만으로 번호를 올리지 않는다. CHANGELOG는 간결한 기록체로 작성한다.
- 코드 변경은 영향 범위에 맞춰 검증하고 자동 검사와 실제 앱·물리 키 확인을 구분해 보고한다. 미실행 검사를 통과로 기록하지 않는다.
- 배포와 필수 업데이트 정책은 [배포 안내](docs/RELEASE.md)를 따른다. 원격 변경·배포·정책 발행은 사용자가 요청한 범위에서 수행한다.
- 개발 타임라인은 `docs/private/DEVELOPMENT-TIMELINE.md`에 유지한다. 별도 요청이 없어도 현재 작업에서 중요한 결정·문제 해결·구현 및 검증·출시 이정표나 과거 기록 정정이 생기면 작업을 마칠 때 갱신한다. 기록할 변화가 없는 단순 질문·사소한 수정은 생략하고 같은 사건은 기존 항목에 보완한다. 날짜는 KST, 내용은 이유·결과·근거 중심으로 작성하며 현재 확인한 범위만 반영하고 미확인 날짜는 추정으로 채우지 않는다. 일괄 정리 요청 시에는 마지막 확인 이후 기록을 대조해 누락을 보완한다. 내부 문서가 없으면 공개 CHANGELOG에 사적 대화 기록을 대신 넣지 않는다.

- 문서·릴리스 노트·사이트를 수정하거나 게시하기 전에 대상 서비스의 렌더링 결과를 확인한다. 의도한 강조 대신 `**` 등 Markdown 기호가 본문에 남는지 점검하며, 특히 말줄임표·괄호 등 문장부호와 한글 조사가 붙는 표현은 필요 시 `<strong>` 또는 일반 문구로 수정한다. 원본 구문 검사만으로 표시 검증을 대신하지 않는다.
