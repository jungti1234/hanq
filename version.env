# Release identity is tracked in Git, never inferred from an installed app.
# Increment BUILD_NUMBER for each new distributed build, including betas.
APP_VERSION=0.3.0
BUILD_NUMBER=101
RELEASE_VERSION=0.3.0-beta.1
